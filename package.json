{
  "name": "@weborigami/cat-prints-store",
  "type": "module",
  "dependencies": {
    "@weborigami/origami": "0.1.0"
  },
  "license": "MIT",
  "scripts": {
    "build": "ori 'copy static(src/site.ori), clear files:build'",
    "docs": "ori 'svg docs/diagram.yaml, { createLinks: false }' > docs/site.svg",
    "start": "ori serve watch src, =debug src/site.ori"
  }
}
